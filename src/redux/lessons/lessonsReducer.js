const initialState = {
    lessonsList: []
};

const lessonsReducer = (state = initialState, action) => {
    switch(action.type) {
        default: return state;
    }
};

export default lessonsReducer;